# Build artifacts
/build/
/build-output/
/target/
/enclave/target/
/sysroot/
target/

# NOTE: Cargo.lock is intentionally COMMITTED for reproducible builds.
# Do not add it to .gitignore.

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# SGX signing key (generate locally, never commit)
config/enclave_private.pem
*.signed.so
*.token

# KV store data
/kvdata/

# OS-specific
Thumbs.db
.DS_Store

# Test certificate private keys
*.key
*.crt
*.csr