# Pin the exact Rust nightly used for enclave builds.
# This ensures `rustup` automatically installs the right toolchain.
# Corresponds to: rustc 1.98.0-nightly (8b6558a02 2026-06-20), LLVM 22.1.7
[toolchain]
channel = "nightly-2026-06-21"
components = ["rust-src"]
